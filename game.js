const prompt = require('prompt');
const Player = require('./player');
const Board = require('./board');

class BattleShip {
  constructor() {
    this.playerOne = null;
    this.playerTwo = null;
  }
}
